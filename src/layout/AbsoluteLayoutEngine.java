package layout;

import main.BaseComp;

public class AbsoluteLayoutEngine extends BaseLayoutEngine {
    @Override
    public void doLayout(BaseComp c) {
        // Absolute layout keeps children bounds as-is.
    }
}
